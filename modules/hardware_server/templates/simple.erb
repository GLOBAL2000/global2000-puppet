auto lo
iface lo inet loopback

auto <%= interfaces %>
iface <%= interfaces %> inet static
        address <%= ip %>
        netmask <%= netmask %>
        network <%= network %>
        broadcast <%= broadcast %>
        gateway <%= gateway %>
